<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <defs>
    <radialGradient id="centerBall" cx="0.35" cy="0.35" r="0.65">
      <stop offset="0%" stop-color="#E74C3C"/>
      <stop offset="100%" stop-color="#8B1F18"/>
    </radialGradient>
  </defs>

  <!-- Tile body — square app icon -->
  <rect width="512" height="512" rx="80" fill="#FAF7EC" stroke="#C9BC8A" stroke-width="6"/>

  <!-- Corner number -->
  <text x="38" y="118" font-family="Georgia,serif" font-weight="900" font-size="100" fill="#C0392B">1</text>

  <!-- 1 Dot medallion -->
  <!-- Outer red ring -->
  <circle cx="256" cy="290" r="170" fill="#C0392B"/>
  <!-- Ivory gap -->
  <circle cx="256" cy="290" r="150" fill="#FAF7EC"/>
  <!-- Black ring -->
  <circle cx="256" cy="290" r="130" fill="#1A1A1A"/>
  <!-- Inner ivory fill -->
  <circle cx="256" cy="290" r="110" fill="#FAF7EC"/>
  <!-- Center red ball -->
  <circle cx="256" cy="290" r="58" fill="url(#centerBall)"/>
</svg>
